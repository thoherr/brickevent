<h1>Benutzeranmeldung</h1>

<div class="InputForm">

<p>Zur Anmeldung musst Du mindestens Deine Email-Adresse, ein selbstgewähltes Passwort und Deinen Namen angeben.<br/>
Alle anderen Angaben sind optional.</p>

<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">Email (gleichzeitig Dein Login)<br />
  <%= f.email_field :email %></div>

  <div class="field">Passwort<br />
  <%= f.password_field :password %></div>

  <div class="field">Passwort bitte wiederholen<br />
  <%= f.password_field :password_confirmation %></div>

  <div class="field">
    Vollständiger Name<br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    LUG (z. B. Bricking Bavaria, 1000Steine, MBFR, SwissLUG, ...)<br />
    <%= f.text_field :lug %>
  </div>
  <div class="field">
    Nickname<br />
    <%= f.text_field :nickname %>
  </div>
  <div class="field">
    Adresse (optional)<br />
    <%= f.text_area :address %>
  </div>
  <div class="field">
    Telefonnummer (optional)<br />
    <%= f.text_field :phone %>
  </div>

  <div class="actions">
  <%= f.submit "Als Benutzer registrieren" %>
  </div>
  
<% end %>

</div>
<%= render :partial => "devise/shared/links" %>
