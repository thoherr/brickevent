<%= form_for(@attendee_type) do |f| %>
  <% if @attendee_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attendee_type.errors.count, "error") %> prohibited this attendee_type from being saved:</h2>

      <ul>
      <% @attendee_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
