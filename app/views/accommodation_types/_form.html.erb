<%= form_for(@accommodation_type) do |f| %>
  <% if @accommodation_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accommodation_type.errors.count, "error") %> prohibited this accommodation_type from being saved:</h2>

      <ul>
      <% @accommodation_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :size %><br />
    <%= f.text_field :size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
