<!DOCTYPE html>
<% titleline = "#{SoftwareVersion.app_name} - ADMIN" %>
<html>
<head>
  <title><%= titleline %></title>
  <meta name="application-name" content="BrickEvent Administration"> 
  <meta name="description" content="Veranstaltungen von Bricking Bavaria"> 
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= active_scaffold_includes %>
  <%= csrf_meta_tag %>
</head>
<body>

    <div id="mainTitle">
      <span class="VersionText"><%= "Version #{SoftwareVersion.release_number} (#{SoftwareVersion.build_number})" %></span>
      <span class="TitleText"><%= titleline %></span>
    </div>

    <div id="mainNavi">
        <%= render :partial => "shared/admin_navi" %>
    </div>

    <div id="mainContent">
        <%= render :partial => "shared/flashes" %>
        <%= yield %>
    </div>

</body>
</html>
