<h1>MOC-Daten für <%= @exhibit.event_title %> ändern</h1>

<%= render 'form' %>

<div class="OverviewLink"><%= link_to 'Zurück zur Übersichtsseite', session[:return_to] || attendance_path(@exhibit.attendance_id) %></div>
